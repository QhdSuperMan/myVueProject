import Vue from 'vue'
import Router from 'vue-router'
import App from '../App.vue'
//import exact from '@/components/ds/exact'

Vue.use(Router)

export default new Router({
	mode:'history',
	routes:[
		
	]
})
